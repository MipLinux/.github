# 协作说明

本文件说明 MipLinux 的协作约定。

项目目前规模很小 —— 两位长期开发者，一位使用 CachyOS，一位使用 Arch。约定写得具体一点，是为了避免以后人多了再补。

---

## 先读这个

如果你刚接触这个项目，按这个顺序读：

| 顺序 | 文档 | 内容 |
|---|---|---|
| 1 | `docs/knowledge/01-概念模型.md` | 构建发行版到底在做什么，核心心智模型 |
| 2 | `docs/knowledge/02-环境与工具链.md` | 三个环境的分工、每样工具是什么 |
| 3 | `docs/knowledge/03-项目结构.md` | `profile/` 里每个文件是什么、为什么必须这么组织 |
| 4 | `docs/knowledge/04-架构决策.md` | NVIDIA 与中文方案的具体技术决策 |
| 5 | `docs/knowledge/05-测试方法.md` | 怎么验证 ISO 和安装结果 |
| 6 | `docs/knowledge/06-待定事项.md` | 尚未决定的问题 |

这些文档在项目主仓库中。**不要跳过第 1 篇** —— 它解释了为什么 `mkarchiso` 不是打包工具，以及为什么某些看起来多余的环境隔离是必需的。

---

## 环境

**你的宿主发行版与项目无关。** 构建环境由 `systemd-nspawn` 提供，容器内是纯 Arch；测试环境是 QEMU。

| 角色 | 环境 | 要求 |
|---|---|---|
| 构建 | `systemd-nspawn` 容器内的纯 Arch | 必须能运行 `pacman` / `pacstrap` / `mkinitcpio` |
| 测试 | 宿主机 QEMU + KVM | 必须支持 UEFI（OVMF）引导 |
| 终验 | 真机 | NVIDIA 驱动与畸形分区只能在这里验 |

**不要**在宿主机直接运行 `mkarchiso` —— 它会静默引入宿主发行版的软件源。

操作台脚本 `scripts/mipl.sh` 负责环境自检、构建、QEMU 测试与容器进出：

```bash
sudo ./scripts/mipl.sh doctor   # 换机器第一件事：环境自检
sudo ./scripts/mipl.sh build    # 构建 ISO
sudo ./scripts/mipl.sh qemu     # 刷新 OVMF 变量并启动 QEMU
sudo ./scripts/mipl.sh stop     # 关闭构建容器
```

> **为什么一律要求 `root`：** 这些命令要动构建容器、OVMF 固件和 `out/` 里的产物。
> 不提供「部分命令自动 sudo」—— 同一件事一会儿降权一会儿提权，出问题时
> 分不清是谁的权限在起作用。所以非 root 会被**直接拒绝**，而不是隐式提权。
>
> 因此脚本里的路径一律**从自身位置推导**，固件路径一律**运行时探测**。
> 不要写死家目录，也不要把带空格的路径拆行 —— 这两类问题都已经踩过一次。

---

## 唯一需要保持一致的接口

**包清单文件 `profile/packages.x86_64`。**

Live 环境与安装器写盘时使用同一份清单，这是为了防住「Live 里能用、装完不能用」这一类不一致。改动它等于同时改动两个环境的产物，所以：

- 增删包必须在描述里说明**为什么**，以及影响的是 Live、装后系统，还是两者
- 如果一个包只为 Live 环境所需，考虑它是否应该放在别处

---

## 工作流

### 分支

`main` 保持可构建。改动走分支，然后开 PR。

```bash
git switch -c feat/nvidia-open
# ... 改动 ...
git push -u origin feat/nvidia-open
```

### 提交信息

用中文写清楚「做了什么」和「为什么」。第一行不超过 50 字左右，需要展开就空一行写正文。

```
修正 systemd-nspawn 命令错误的问题

文档里把 -D 写成了 -d，按原文执行会失败。
```

### PR

PR 描述里请写清三件事：

1. **改了什么** —— 涉及的目录或文件
2. **怎么验的** —— `mipl.sh build` 通过？QEMU 能引导？还是只在真机验证过？
3. **没验什么** —— 明确说出没覆盖到的部分，比让人猜有价值

### 提交前自检

- [ ] `sudo ./scripts/mipl.sh build` 能跑通
- [ ] 涉及引导的改动，在 QEMU 里实际引导过一次
- [ ] 涉及 NVIDIA、分区、真机硬件行为的改动，注明是否需要真机验证
- [ ] 新增的技术事实有实测依据，而不是凭记忆写的
- [ ] 没有提交构建产物（`out/`、`*.iso` 已在 `.gitignore` 中）

---

## 文档约定

这个项目的文档是**技术资产**，不是附属品。修改行为时同步修改文档。

| 约定 | 说明 |
|---|---|
| **实测优先** | 包版本、依赖关系、工具行为，以实际查询或阅读源码的结果为准，不写「应该」「大概」 |
| **记录否决的方案** | 已排除的选项要留下理由，避免同一个话题反复讨论 |
| **决策编号** | 已定决策编号为 `Dn`，待定事项编号为 `Pn`，在 `docs/knowledge/` 中展开 |
| **配置即事实** | 文档与配置冲突时以配置为准，然后修文档 |

如果你发现文档与实际情况不符，**修文档和报告问题都算贡献**。这个项目里文档写错的代价，比代码写错更高 —— 因为两位开发者在两台不同的机器上照着它操作。

---

## 报告问题

- **功能异常、文档错误、想法建议** → 用仓库的 Issue 模板
- **安全漏洞** → 见 [SECURITY.md](SECURITY.md)，不要在公开 Issue 里披露

---

## 许可

项目尚未选定开源许可证。在选定之前，请把本仓库的内容视为**保留所有权利**，不要对外分发或用于衍生作品。
