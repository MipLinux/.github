# 安全策略

本文件说明如何报告 MipLinux 的安全问题。

---

## 报告方式

**不要在公开 Issue 中披露安全漏洞。**

请通过以下方式私下报告：

1. **GitHub 私密漏洞报告**（推荐）
   在本组织的任一仓库页面进入 **Security** → **Report a vulnerability**。
   这条通道只有维护者可见。

2. **邮件**
   如果上述通道不可用，请发邮件至 **miprota@163.com**，标题以 `[SECURITY]` 开头。

### 请包含

- 受影响的组件与环境（构建链路 / Live 环境 / 安装后的系统）
- 问题的具体描述与可复现步骤
- 影响范围（能否提权、能否持久化、是否需要本地访问）
- 如果有，你建议的修复方向

### 请不要

- 在公开 Issue、讨论区或社交媒体上披露细节，直到修复发布
- 在报告里附带真实的攻击载荷 —— 概念验证足够说明问题
- 对非你本人拥有的系统进行测试

我们会尽快确认收到，并在修复完成后与你协商披露时间。

---

## 当前状态

> **本项目处于基线阶段，尚未对外发布任何成品 ISO。**
>
> 因此目前没有受支持版本，也没有需要用户升级的安全更新。

现阶段最值得关注的问题集中在**构建链路本身**：

| 范围 | 说明 |
|---|---|
| **构建环境** | `mkarchiso` 与打包流程、`pacman.conf` 与镜像源配置的完整性问题 |
| **CI 工作流** | 仓库中 GitHub Actions 工作流的权限边界与密钥处理 |
| **`scripts/mipl.sh`** | 操作台脚本以 `root` 运行，涉及路径推导、容器管理与固件处理 |
| **配置层** | `profile/airootfs/` 下的默认配置与权限设置 |

---

## 本项目尚未提供的安全机制

为避免误解，明确列出**目前没有**的东西：

| | 状态 |
|---|---|
| 发布产物签名 | ❌ 尚未实现。将来的对外版本计划提供 GPG 签名与校验和 |
| 独立软件仓库 | ❌ 不存在。项目不维护自有仓库 |
| 安全更新通道 | ❌ 尚未建立 |
| 硬件兼容性承诺 | ❌ 尚未做出。NVIDIA 覆盖范围为 Turing 及更新架构，**GTX 10 系及更早的显卡不受支持** |

在签名与校验和机制建立之前，**不要从官方仓库以外的任何来源获取 MipLinux 镜像**。

---

## 关于 NVIDIA 驱动

项目使用官方仓库的 `nvidia-open` 包，不分发任何 NVIDIA 专有二进制。

如果你发现的是 **NVIDIA 驱动本身**的问题（而非本项目的集成方式），请向对应的上游报告：

- Arch Linux：<https://gitlab.archlinux.org/archlinux/packaging/packages>
- NVIDIA 开源内核模块：<https://github.com/NVIDIA/open-gpu-kernel-modules>

---

## 安全模型说明

MipLinux 是一个**装到硬盘的传统发行版**。这意味着：

- 系统安全边界由 Arch Linux 本身提供，本项目不额外加固内核
- Live 环境**必须配置自动登录**（Live 介质不应要求输入密码），因此 Live 环境**不应被视为可信环境**
- 本项目对 NVIDIA 的定位是「开箱可用」，不做额外的驱动沙箱或权限隔离

如果你认为以上任一条本身就是问题，欢迎报告 —— 我们愿意听。
